# Object-detection-with-Webcam
Live object detection using a webcam captures real-time video frames and runs them through a trained model like YOLO or SSD. The model detects objects, draws bounding boxes, and shows labels with confidence scores on each frame. OpenCV handles the camera stream, enabling real-time AI vision applications like surveillance and tracking.
